<nav class="navbar navbar-expand-lg custom-navbar">
  <div class="container align-items-center">
    <%= link_to apartments_path, class: "navbar-brand" do %>
      <span class="visually-hidden">Go to Home</span>
      <%= image_tag "logo2.png", alt: "Site Logo", class: "logo" %>
    <% end %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
<li class="nav-item"><a class="nav-link" href="<%= apartments_path %>">Home</a></li>
<li class="nav-item"><a class="nav-link" href="<%= blog_path %>">Blog</a></li>
<li class="nav-item"><a class="nav-link" href="<%= contact_path %>">Contact</a></li>
<li class="nav-item"><a class="nav-link" href="<%= about_path %>">About Us</a></li>

        <% if user_signed_in? && current_user.role == 'host' %>
            <li class="nav-item d-lg-none mt-2">
                <%= link_to "Add Apartment", new_apartment_path, class: "nav-link custom-link-accent" %>
            </li>
        <% end %>
      </ul>

      <div class="d-flex align-items-center gap-3 user-action-area">

        <% if user_signed_in? && current_user.role == 'host' %>
          <%= link_to "Add Apartment", new_apartment_path, class: "custom-link-accent d-none d-lg-block" %>
        <% end %>

        <% if user_signed_in? %>
          <div class="btn-group">
            <div class="profile-icon dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false" aria-haspopup="true">
               <i class="bi bi-person"></i>
            </div>

            <ul class="dropdown-menu dropdown-menu-end">
             <h6 class="dropdown-header fw-bold">Welcome, <%= current_user.email.split('@').first %></h6>

              <h6 class="dropdown-header small text-muted">Role: <%= current_user.role.to_s.capitalize %></h6>

              <li><hr class="dropdown-divider"></li>

              <li><%= link_to "Profile", profile_path, class: "dropdown-item" %></li>

              <li><%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "dropdown-item custom-logout-item" %></li>
            </ul>
          </div>
        <% else %>
          <%= link_to "Login", new_user_session_path, class: "btn btn-sm custom-btn-link" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
